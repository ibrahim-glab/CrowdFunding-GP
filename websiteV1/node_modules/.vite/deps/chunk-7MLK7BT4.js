import {
  assertWindowEthereum
} from "./chunk-RKAC5DGX.js";

// node_modules/@thirdweb-dev/wallets/dist/getInjectedMetamaskProvider-d2e9e4f3.browser.esm.js
function getInjectedMetamaskProvider() {
  var _a;
  function getReady(ethereum) {
    const isMetaMask = !!(ethereum == null ? void 0 : ethereum.isMetaMask);
    if (!isMetaMask) {
      return;
    }
    if (ethereum.isBraveWallet && !ethereum._events && !ethereum._state) {
      return;
    }
    if (ethereum.isRainbow) {
      return;
    }
    if (ethereum.isPhantom) {
      return;
    }
    if (ethereum.isAvalanche) {
      return;
    }
    if (ethereum.isBitKeep) {
      return;
    }
    if (ethereum.isMathWallet) {
      return;
    }
    if (ethereum.isZerion) {
      return;
    }
    if (ethereum.isKuCoinWallet) {
      return;
    }
    if (ethereum.isPortal) {
      return;
    }
    if (ethereum.isTokenPocket) {
      return;
    }
    if (ethereum.isTokenary) {
      return;
    }
    if (ethereum.isRabbyWallet) {
      return;
    }
    return ethereum;
  }
  if (assertWindowEthereum(globalThis.window)) {
    if ((_a = globalThis.window.ethereum) == null ? void 0 : _a.providers) {
      return globalThis.window.ethereum.providers.find(getReady);
    }
    return getReady(globalThis.window.ethereum);
  }
}

export {
  getInjectedMetamaskProvider
};
//# sourceMappingURL=chunk-7MLK7BT4.js.map
